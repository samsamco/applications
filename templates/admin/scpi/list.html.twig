{% extends 'admin/base.html.twig' %}

{% block title %} Scpi {% endblock %}

{% block extrahead %}


{% endblock %}


{% block extrafooter %}

{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card card-table">
                <div class="card-header">Scpi</div>
                <div class="card-body">
                    <table class="table">
                        <thead>
                        <tr>
                            <th>Nom</th>
                            <th>Année</th>
                            <th>Rendement actuel</th>
                            <th>Valeur part</th>
                            <th>Bilan</th>
                            <th>Gestionnaire</th>
                            <th class="actions"></th>
                        </tr>
                        </thead>
                        <tbody>
                            {% for s in scpis %}
                                <tr>
                                    <td> {{ s.nom }}</td>
                                    <td> {{ s.annee }} </td>
                                    <td> {{ s.rendementactuel }} </td>
                                    <td> {{ s.valeurPart }} </td>
                                    <td> <a href="/admin/bilan/edit/{{ s.Bilan.id }}"> {{ s.Bilan.url }} </a> </td>
                                    <td> <a href="/admin/gestionnaire/edit/{{ s.MyGestionnaire.id }}"> {{ s.MyGestionnaire.nom }} </a> </td>
                                    <td class="actions">
                                        <a class="icon" href="/admin/scpi/delete/{{ s.id }}"><i class="mdi mdi-delete"></i></a>&nbsp;&nbsp;
                                        <a class="icon" href="/admin/scpi/edit/{{ s.id }}"><i class="mdi mdi-edit"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}

                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    </div>
{% endblock %}
